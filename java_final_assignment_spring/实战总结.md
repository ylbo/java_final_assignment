# 获取bean对象
之前跟着视频写多了，都是写在测试单元中，都忘记了在正常情况
怎么从IOC容器中获取对象。正常情况要根据配置文件创建
applicationContext

# 关于配置文件
使用java配置文件尤其是配置数据库连接池，实在是麻烦，觉得xml加
注解的方式更好

# 注入方式
最好统一使用一种配置方式，setter注入优先，其次是构造器注入

# 对象之间的关系
不要弄出循环依赖关系，开始写配置文件头都绕晕了
